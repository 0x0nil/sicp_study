;; 练习1.10 下面过程计算一个称为Ackermann函数的数学函数:

(define (A x y)
  (cond ((= x y) 0)
        ((= x 0) (* x y))
        ((= y 1) 2)
        (else (A (- x 1)
                 (A x (- y 1))))))

;; 下面个表达式的值是什么：

(A 1 10)
(A 2 4)
(a 3 3)

